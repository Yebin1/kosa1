import kr.or.kosa.SingletonHelper;

public class Ex04_SingletoneHelper {

	public static void main(String[] args) {

		// singleton 사용시
		// 하나의 객체를 사용
		// 하나의 객체를 공유하여 사용

		// 공유 자원이므로 APP이 종료될 때까지 close()하지 않는 것이 원칙

	//	SingletonHelper helper = SingletonHelper.getInstance();
	//	System.out.println(helper.toString());

	//	SingletonHelper helper2 = SingletonHelper.getInstance();
	//	System.out.println(helper2.toString());

	}

}
